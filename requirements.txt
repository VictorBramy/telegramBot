# Core dependencies - guaranteed to work
python-telegram-bot==21.0
python-dotenv==1.0.0
requests==2.31.0

# Financial data (optional - will skip if fails)
yfinance==0.2.18
pandas==2.1.4
numpy==1.24.3

# Analysis (optional - will skip if fails) 
scikit-learn==1.3.2